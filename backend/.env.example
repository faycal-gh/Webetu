# Progress API - Environment Variables Configuration
# Copy this file to .env and update with your actual values
# DO NOT commit .env file to version control!

# ============================================
# REQUIRED CONFIGURATION
# ============================================

# JWT Secret Key (REQUIRED for production)
# Generate with: openssl rand -base64 32
# This is used to sign and verify JWT tokens
JWT_SECRET=CHANGE_ME_IN_PRODUCTION_USE_OPENSSL_RAND_BASE64_32

# CORS Allowed Origins (REQUIRED for production)
# Comma-separated list of allowed frontend URLs
# Examples:
#   - Development: http://localhost:3000,http://localhost:5173
#   - Production: https://yourdomain.com,https://www.yourdomain.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Server Configuration
# Port the application will run on
PORT=8080

# Spring Profile
# Options: default (development), prod (production)
SPRING_PROFILES_ACTIVE=default

# External API Configuration
# Base URL for the Progres API (Algeria's student management system)
EXTERNAL_API_BASE_URL=https://progres.mesrs.dz/api

# External API timeout in milliseconds (default: 10 seconds)
EXTERNAL_API_TIMEOUT=10000

# ============================================
# RATE LIMITING CONFIGURATION
# ============================================

# Maximum number of requests allowed per time window
RATE_LIMIT_REQUESTS=100

# Time window for rate limiting in minutes
RATE_LIMIT_WINDOW_MINUTES=15

# ============================================
# JWT CONFIGURATION
# ============================================

# JWT access token expiration in milliseconds
# Default: 900000 (15 minutes)
JWT_EXPIRATION=900000

# JWT refresh token expiration in milliseconds
# Default: 604800000 (7 days)
JWT_REFRESH_EXPIRATION=604800000

# ============================================
# CORS CONFIGURATION
# ============================================

# Allowed HTTP methods (comma-separated)
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS

# CORS max age in seconds (how long preflight requests are cached)
CORS_MAX_AGE=3600

# ============================================
# GROQ AI CONFIGURATION (for recommendations)
# ============================================

# Groq API Key (REQUIRED for AI-powered recommendations)
# Get your free API key from: https://console.groq.com/keys
# Without this, the recommendation feature will not work
GROQ_API_KEY=your_groq_api_key_here

# Groq Model (optional, default: llama-3.3-70b-versatile)
# Available models: llama-3.1-70b-versatile, llama-3.1-8b-instant, mixtral-8x7b-32768
# GROQ_MODEL=llama-3.3-70b-versatile

# Groq API timeout in milliseconds (default: 30 seconds)
# GROQ_TIMEOUT=30000

# ============================================
# DEPLOYMENT CONFIGURATION (for Koyeb, etc.)
# ============================================

# Koyeb API Token (only needed for CI/CD deployment)
# Get from: https://app.koyeb.com/account/api
# KOYEB_TOKEN=your_koyeb_api_token

# Koyeb App URL (for health checks in CI/CD)
# KOYEB_APP_URL=https://your-app.koyeb.app

# ============================================
# LOGGING CONFIGURATION (Optional)
# ============================================

# Log level for the application
# Options: TRACE, DEBUG, INFO, WARN, ERROR
# LOGGING_LEVEL_ROOT=INFO
# LOGGING_LEVEL_COM_PROGRESS=DEBUG

# ============================================
# NOTES
# ============================================

# 1. Security Best Practices:
#    - Never commit .env file to version control
#    - Use strong, randomly generated JWT_SECRET in production
#    - Rotate JWT secrets periodically
#    - Use HTTPS in production for CORS origins
#
# 2. Development vs Production:
#    - Development: Use .env file locally
#    - Production: Set environment variables in your hosting platform
#
# 3. Generating Secure Secrets:
#    - JWT Secret: openssl rand -base64 32
#    - Strong Password: openssl rand -base64 24
#
# 4. Platform-Specific Variables:
#    - Some platforms (Heroku, Railway) automatically set PORT
#    - Always check your platform's documentation
